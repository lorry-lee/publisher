server {
        listen 8000;
        server_name localhost;
        location / {
                include uwsgi_params;
                uwsgi_pass 127.0.0.1:8630;
        }
}

server {
        listen 9000;
        server_name localhost;
        location / {
                include uwsgi_params;
                uwsgi_pass 127.0.0.1:8640;
        }
}
